#!/bin/bash
# PreCompact hook: fires before context compaction.
# Tells Claude to save important notes before context is lost.

echo "COMPACTION IMMINENT. Run the /narrative skill NOW so the current session lands in the project narrative before context is lost."
